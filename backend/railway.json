{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "context": ".",
    "dockerfile": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'prisma migrate resolve --applied init 2>/dev/null; prisma migrate deploy && node dist/src/server.js'",
    "restartPolicy": "always",
    "numReplicas": 1
  }
}
